[build-system]
requires = ["setuptools>=42"]
build-backend = "setuptools.build_meta"

[project]
name = "ritam"
version = "1.1.1"
description = "RITAM governed cognition substrate — v1.1.1"
requires-python = ">=3.9"

[tool.setuptools.packages.find]
where = ["."]
include = ["ritam*"]
